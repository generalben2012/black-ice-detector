# 1단계: 프로젝트 생성 및 디렉토리 구조 설정

이 문서는 블랙아이스 감지 프로젝트를 처음부터 만드는 과정을 설명합니다.

## 목표

Arduino UNO Q와 HC-SR04 초음파 센서, LDR 조도 센서를 사용하여 블랙아이스를 감지하고 웹에 표시하는 프로젝트의 기본 구조를 생성합니다.

## 필요한 것

- Arduino App Lab이 설치된 컴퓨터
- Arduino UNO Q
- HC-SR04 초음파 센서
- LDR 조도 센서
- 10kΩ 저항 (LDR 분압 회로용)
- 점퍼 와이어 6개

## 단계별 가이드

### 1. 프로젝트 디렉토리 생성

프로젝트를 저장할 디렉토리를 생성합니다. `uno-q-pin-toggle`과 같은 레벨에 생성합니다.

```bash
mkdir -p black-ice-detector/{python,sketch,assets/{img,libs,fonts}}
```

**디렉토리 구조 설명:**
- `python/`: Python 백엔드 코드 저장
- `sketch/`: Arduino 스케치 코드 저장
  - `sketch.ino`: 메인 스케치 파일
  - `pulseInCustom.h`, `pulseInCustom.cpp`: Zephyr용 pulseIn 함수 구현
- `assets/`: 웹 인터페이스 파일 저장
  - `img/`: 이미지 파일
  - `libs/`: JavaScript 라이브러리 (Socket.IO 등)
  - `fonts/`: 폰트 파일

### 2. app.yaml 파일 생성

프로젝트의 메타데이터와 사용할 Bricks를 정의하는 파일입니다.

**파일 위치:** `black-ice-detector/app.yaml`

**내용:**
```yaml
name: Black Ice Detector
description: Detect black ice using HC-SR04 ultrasonic sensor and display on web interface
ports: []
bricks:
- arduino:web_ui: {}
icon: 🧊
```

**설명:**
- `name`: 프로젝트 이름
- `description`: 프로젝트 설명 (HC-SR04 초음파 센서와 LDR 조도 센서 사용)
- `bricks`: 사용할 Arduino App Bricks 목록
  - `web_ui`: 웹 인터페이스를 제공하는 Brick
- `icon`: 프로젝트 아이콘 (이모지)

### 3. 기본 파일 구조 확인

프로젝트 디렉토리 구조가 올바르게 생성되었는지 확인합니다:

```
black-ice-detector/
├── app.yaml
├── python/
│   └── main.py          # Python 백엔드 (나중에 생성)
├── sketch/
│   ├── sketch.ino       # Arduino 스케치 (나중에 생성)
│   ├── pulseInCustom.h  # Zephyr용 pulseIn 헤더 (나중에 생성)
│   └── pulseInCustom.cpp # Zephyr용 pulseIn 구현 (나중에 생성)
└── assets/
    ├── index.html       # 웹 인터페이스 HTML (나중에 생성)
    ├── app.js          # 웹 인터페이스 JavaScript (나중에 생성)
    ├── style.css       # 웹 인터페이스 CSS (나중에 생성)
    ├── img/
    ├── libs/
    │   └── socket.io.min.js  # Socket.IO 라이브러리 (나중에 추가)
    └── fonts/
```

**주요 파일 설명:**
- `app.yaml`: 프로젝트 메타데이터 및 Bricks 설정
- `python/main.py`: Python 백엔드 (Bridge 통신 및 WebSocket 서버)
- `sketch/sketch.ino`: Arduino 스케치 (HC-SR04 및 LDR 센서 제어)
- `sketch/pulseInCustom.*`: Zephyr RTOS용 pulseIn 함수 구현
- `assets/index.html`: 웹 인터페이스 HTML 구조
- `assets/app.js`: WebSocket 통신 및 UI 업데이트 로직
- `assets/style.css`: 웹 인터페이스 스타일

## 프로젝트 개요

이 프로젝트는 다음 센서들을 사용합니다:

1. **HC-SR04 초음파 센서**: 도로 표면과의 거리를 측정하여 블랙아이스 감지
2. **LDR 조도 센서**: 주변 밝기를 측정하여 환경 정보 제공

센서 데이터는 Arduino UNO Q의 Bridge 기능을 통해 Python 백엔드로 전송되고, WebSocket을 통해 웹 인터페이스에 실시간으로 표시됩니다.

## 다음 단계

프로젝트 구조가 생성되었으므로 다음 단계로 진행합니다:

- [2단계: Arduino 스케치 작성](02-Arduino-스케치-작성.md) - HC-SR04 및 LDR 센서 제어 코드 작성
- [3단계: Python 백엔드 작성](03-Python-백엔드-작성.md) - Bridge 통신 및 WebSocket 서버 구현
- [4단계: 웹 인터페이스 작성](04-웹-인터페이스-작성.md) - HTML, CSS, JavaScript로 UI 구현
- [5단계: 센서 연결 및 테스트](05-센서-연결-및-테스트.md) - 하드웨어 연결 및 전체 시스템 테스트

## 체크리스트

- [ ] 프로젝트 디렉토리 생성 완료
- [ ] app.yaml 파일 생성 완료
- [ ] 디렉토리 구조 확인 완료
- [ ] 필요한 부품 목록 확인 완료 (HC-SR04, LDR, 저항 등)

## 문제 해결

### 디렉토리 생성 오류

**문제:** 디렉토리를 생성할 수 없습니다.

**해결:**
- 권한 확인: 디렉토리를 생성할 권한이 있는지 확인합니다.
- 경로 확인: 올바른 경로에 있는지 확인합니다.

### app.yaml 파일 인식 안 됨

**문제:** Arduino App Lab에서 프로젝트를 인식하지 못합니다.

**해결:**
- 파일 위치 확인: `app.yaml` 파일이 프로젝트 루트 디렉토리에 있는지 확인합니다.
- YAML 문법 확인: 들여쓰기와 문법이 올바른지 확인합니다.
- 프로젝트 새로고침: Arduino App Lab에서 프로젝트 목록을 새로고침합니다.

## 참고 사항

### 프로젝트 이름 변경

프로젝트 이름을 변경하려면 `app.yaml` 파일의 `name` 필드를 수정합니다:

```yaml
name: My Custom Project Name
```

### 추가 Bricks 사용

다른 Bricks를 사용하려면 `app.yaml`의 `bricks` 섹션에 추가합니다:

```yaml
bricks:
- arduino:web_ui: {}
- arduino:other_brick: {}
```

### 프로젝트 아이콘 변경

프로젝트 아이콘을 변경하려면 `app.yaml` 파일의 `icon` 필드를 수정합니다:

```yaml
icon: 🚗  # 다른 이모지 사용 가능
```

